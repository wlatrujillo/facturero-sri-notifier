// const cdk = require('aws-cdk-lib/core');
// const { Template } = require('aws-cdk-lib/assertions');
// const FactureroSriNotifier = require('../lib/facturero-sri-notifier-stack');

// example test. To run these tests, uncomment this file along with the
// example resource in lib/facturero-sri-notifier-stack.js
test('SQS Queue Created', () => {
//   const app = new cdk.App();
//   // WHEN
//   const stack = new FactureroSriNotifier.FactureroSriNotifierStack(app, 'MyTestStack');
//   // THEN
//   const template = Template.fromStack(stack);

//   template.hasResourceProperties('AWS::SQS::Queue', {
//     VisibilityTimeout: 300
//   });
});
